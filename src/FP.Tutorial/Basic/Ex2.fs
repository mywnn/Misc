namespace FP.Tutorial.Basic

open System
open System.IO

module Ex2 =
    //complete the following functions
    //try making the code reusable.
    let printFormat1ToConsole (lines: string list) = ()
    let printFormat2ToConsole (lines: string list) = ()
    let printFormat1ToFile (lines: string list) = ()
    let printFormat2ToFile (lines: string list) = ()