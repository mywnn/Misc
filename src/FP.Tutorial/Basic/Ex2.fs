namespace FP.Tutorial.Basic

open System
open System.IO

module Ex2 =
    //complete the following functions
    //try making the code reusable.
    let printFormat1ToConsole (content: string list) = ()
    let printFormat2ToConsole (content: string list) = ()
    let printFormat1ToFile (content: string list) = ()
    let printFormat2ToFile (content: string list) = ()